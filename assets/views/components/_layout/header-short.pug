mixin header-short(mod)
  header.header.short(class = mod ? `header_${mod}` : false)
    .header__cont.cont
      .header__layer
      .header__area
        .header__logo
          img.header__logo-image(src=staticImg('logo.svg') alt='')
        if mod === 'quiz'
          a.header__offer-consult.btn-orange.btn-orange_arrow(href='#modal-order-new' data-banner="banner-id-1" data-ftitle="Связаться с нами" data-fancybox data-options='{"touch": false}') Связаться с нами
